services:
    - type: web
      name: website
      runtime: docker
      dockerfilePath: ./Dockerfile
      dockerContext: .
      plan: free
      # PORT is automatically set by Render, no need to override
      healthCheckPath: /health
